# Puerto del servidor (Vercel lo configura automáticamente)
PORT=3000

# Clave secreta para tokens JWT (¡CAMBIAR EN PRODUCCIÓN!)
# Genera una clave segura con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=tu-clave-secreta-aqui-cambiala-en-produccion